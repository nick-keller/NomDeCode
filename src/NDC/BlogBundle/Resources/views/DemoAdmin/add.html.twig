{% extends 'NDCBlogBundle:DemoAdmin:layout.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    {{ helper.breadcrumb(null, 'Ajouter') }}
{% endblock %}

{% block body %}
    <h1>Ajouter une démo</h1>

    {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
	{{ form_start(form) }}
	    {{ form_widget(form) }}

	<div class="row">
	    <div class="col-sm-10 col-sm-offset-2">
	        <div class="btn-group">
	            <button class="btn btn-default" type="submit">
	                Enregistrer
	            </button>
	        </div>
	    </div>
	</div>
	{{ form_end(form) }}
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	{% include 'NDCBlogBundle:DemoAdmin:_dependencies_shortcuts.html.twig' %}
{% endblock %}