{% extends '@NDCUser/UserAdmin/layout.html.twig' %}

{% block body %}
    <table class="table table-bordered table-striped table-condensed table-hover">
        <tr>
            <th>ID</th>
            <th>Pseudo</th>
            <th>Email</th>
            <th>Dernière connexion</th>
            <th>Actions</th>
        </tr>
        {% for user in users %}
            <tr>
                <td>
                    {{ user.id }}
                </td>
                <td>
                    {{ user }}
                </td>
                <td>{{ user.email }}</td>
                <td>{{ user.lastLogin|fullDate }}</td>
                <td>
                    <a href="{{ path('blog_user_admin_edit', {id:user.id}) }}" class="btn btn-default btn-xs">
                        <i class="glyphicon glyphicon-pencil"></i> Editer
                    </a>
                </td>
            </tr>
        {% endfor %}
    </table>

    {{ knp_pagination_render(users) }}

{% endblock %}